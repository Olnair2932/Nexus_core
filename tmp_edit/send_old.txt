                if(data.url) {
                    document.getElementById('track-info').innerText = "PLAYING: " + decodeURIComponent(data.url).split('/').pop();
                    const p = document.getElementById('nexusPlayer');
                    p.src = data.url; p.load(); p.play().catch(()=>{});
                }
