services:
  - type: web
    name: nexus-core
    env: node
    buildCommand: |
      npm install
      pip install yt-dlp
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
