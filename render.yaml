services:
  - type: web
    name: nexus-core
    env: node
    buildCommand: |
      npm install
      python3 -m pip install --user yt-dlp
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
