services:
  - type: web
    name: nexus-core
    env: node
    buildCommand: |
      npm install
      apt-get update && apt-get install -y deno
      python3 -m pip install yt-dlp
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
